export const useStep = ({
  step,
  index,
  length,
}: {
  step: any;
  index: number;
  length: number;
}) => {
  console.log("step", step);
  console.log("index", index);
  console.log("leng0", length);
  const a = 1;
  return { a };
};
